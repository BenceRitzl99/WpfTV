<UserControl x:Class="WpfTV.Pages.StorageTitlePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTV.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal"  >
            
            <Border Background="{Binding Color}" Width="15" Height="15" CornerRadius="2" Margin="0,2,5,0"></Border>
            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="22" Margin="0,20"/>
        </StackPanel>
    </Grid>
</UserControl>
